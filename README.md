# Multi-Agent Pipeline Skill

This repository is the source-of-truth workspace for the `multi-agent-pipeline` skill.

## Layout

- `skills/multi-agent-pipeline/`: the skill itself, including `SKILL.md`, stage prompts, and reference contracts
- `README.md`: repository-level notes
- `.gitignore`: local development and runtime ignores

## Working Rule

Keep skill content inside `skills/`. The repository root should stay light and only hold repo-level files such as `.gitignore`, `README.md`, and similar project metadata.

## Skill Path

The current skill lives at:

`skills/multi-agent-pipeline/`

If you want to install or sync it into Codex discovery later, copy that directory into your Codex skills home.

